extends layout

block content
  div.container
    header
      .block-messages
        ul
      div.header.wide
        - if (typeof user !== 'undefined')
          div.user-box
            a.username.header-action(href="/#{user.username}") #{user.displayName}
            a.header-action(href="javascript:;")
              i.icon-menu-1
            a.header-action(href="/logout")
              i.icon-logout
            a.add-data-button.button(href="javascript:;") Add Data
        - else
          a.signin-button.button(href="javascript:;") Sign in
        div.header-logo
          a.navigate(href="/")
            img(src="#{root}/img/time.png", width="13", height="12")
            | Skylin
        span.header-title
    div.main(role="main")
    footer
