- if (typeof user === "undefined") { user = null; }
- if (typeof stateStr === "undefined") { stateStr = null; }
#loading
#container
  header
    div
      a(href="/")
        img#logo(src="/graphics/skyline.png", alt="Skyline")
    div
      nav
        ul
  #main(role="main", data-state=stateStr, data-user=user)
    div.tabs
      div.tabs-dynamic
      div.tab-bar
    div.folder
  footer

  