diff --git a/node_modules/dnode/node_modules/dnode-protocol/index.js b/node_modules/dnode/node_modules/dnode-protocol/index.js
index 6cb873c..742796a 100644
--- a/node_modules/dnode/node_modules/dnode-protocol/index.js
+++ b/node_modules/dnode/node_modules/dnode-protocol/index.js
@@ -124,8 +124,16 @@ var Session = exports.Session = function (id, wrapper) {
     }
     
     function apply(f, obj, args) {
-        try { f.apply(obj, args) }
-        catch (err) { self.emit('error', err) }
+        try {
+            f.apply(obj, args);
+        } catch (err) {
+            // Work around a Chrome bug where re-throwing an error destroys
+            // the stack trace:
+            //   http://code.google.com/p/chromium/issues/detail?id=60240
+            console.error('dnode callback threw error: ' + err + '\n' +
+                          err.stack);
+            self.emit('error', err);
+        }
     }
     
     return self;
