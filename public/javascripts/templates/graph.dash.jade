.dashboard-item
  - if (title) {
    .dashboard-item-header.taller-header
      .small-button.add-graph(role="button", title="Add graph pane")
        | Add Graph
      .small-button.export(role="button", title="Export visible data as CSV")
        | Export
      .small-button.fetchLatest(role="button", title="Fetch latest data")
        | Refresh
      .small-button-sticky.followLatest(role="button", title="Follow latest data")
        | Follow
      .mouse-time
        span
      - if (shrinkable) {
        a.dashboard-link.toggler(href="javascript:;", title="shrink")
          | -
      - }
      h1.dashboard-item-header-txt
        =title
  - } else {
    .graph-closer
  - }
  .dashboard-item-content
    - if (waiting) {
      p.dashboard-item-message
        | Waiting...
    - } else if (loading) {
      p.dashboard-item-message
        | Loading...
    - } else if (empty) {
      p.dashboard-item-message
        | Nothing to graph.
    - } else {
      .graph
        div
      .notification-preview
    - }

