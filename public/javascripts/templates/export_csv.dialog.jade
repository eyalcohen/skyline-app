#confirm
  .header
    span Export data as CSV
  .message
    p Columns to download:
      ol
        // Why doesn't this work: ???
        // each channelName in channelNames
        - channels.forEach(function(channel) {
          li= channel.title
        - })
    p
      input(type="radio", name="sampleType", value="resample")
      | Resample to:
      input(type="text", value="1.0")#resample
      | seconds per sample.
    // Indent next?
    p
      input(type="checkbox", name="minmax", value="minmax")
      | Include minima and maxima.
    p
      input(type="radio", name="sampleType", value="noResample")
      | Don't resample.
    a#download(href="#") Click to download
  .buttons
    .simplemodal-close Done
